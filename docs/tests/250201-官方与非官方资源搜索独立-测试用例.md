# 官方与非官方资源搜索独立功能 - 测试用例文档

## 📋 测试概述

本文档包含"官方与非官方资源搜索独立"功能的完整测试用例，涵盖单元测试、集成测试和端到端测试场景。

---

## 1. 单元测试

### 1.1 官方资源搜索功能测试 (`searchOfficialResources`)

#### 测试文件：`src/lib/__tests__/search-independent.test.ts`

**用例 1.1.1：成功搜索官方资源**

- **描述**：验证官方资源搜索功能正常工作
- **前置条件**：无
- **测试步骤**：
  1. Mock API 返回有效的搜索结果
  2. 调用 `searchOfficialResources('测试')`
  3. 验证返回结果
- **预期结果**：
  - 返回结果数组，长度 > 0
  - 每个结果包含 `source_type: 'official'`
  - episodes 字段包含非 m3u8 格式的 URL
  - 正确提取所有播放源和剧集 URL

**用例 1.1.2：使用自定义 baseUrl**

- **描述**：验证可以自定义搜索接口 URL
- **测试步骤**：
  1. 调用 `searchOfficialResources('测试', 'https://custom-api.com')`
  2. 验证请求 URL
- **预期结果**：请求发送到自定义 URL

**用例 1.1.3：使用环境变量配置**

- **描述**：验证从环境变量读取配置
- **测试步骤**：
  1. 设置 `NEXT_PUBLIC_OFFICIAL_SEARCH_URL`
  2. 调用搜索函数
  3. 验证请求 URL
- **预期结果**：使用环境变量中的 URL

**用例 1.1.4：处理空结果**

- **描述**：验证 API 返回空列表时的处理
- **测试步骤**：
  1. Mock API 返回空列表
  2. 调用搜索函数
- **预期结果**：返回空数组

**用例 1.1.5：处理 API 错误**

- **描述**：验证 API 返回错误状态码时的处理
- **测试步骤**：
  1. Mock API 返回 500 错误
  2. 调用搜索函数
- **预期结果**：返回空数组，不抛出异常

**用例 1.1.6：处理请求超时**

- **描述**：验证请求超时处理
- **测试步骤**：
  1. Mock fetch 永不返回
  2. 等待超时（5 秒）
  3. 验证结果
- **预期结果**：返回空数组，不抛出异常

**用例 1.1.7：正确提取剧集 URL**

- **描述**：验证从 `vod_play_url` 正确提取所有剧集 URL
- **测试步骤**：
  1. Mock 包含多个播放源和剧集的响应
  2. 调用搜索函数
  3. 验证 episodes 数组
- **预期结果**：正确提取所有剧集 URL

**用例 1.1.8：处理缺失 vod_play_url**

- **描述**：验证当 `vod_play_url` 缺失时的处理
- **测试步骤**：
  1. Mock 响应中 `vod_play_url` 为 undefined
  2. 调用搜索函数
- **预期结果**：episodes 为空数组

---

### 1.2 非官方资源搜索功能测试 (`searchUnofficialResources`)

#### 测试文件：`src/lib/__tests__/search-independent.test.ts`

**用例 1.2.1：成功搜索非官方资源**

- **描述**：验证非官方资源搜索功能正常工作
- **测试步骤**：
  1. Mock API 返回有效的搜索结果
  2. 调用 `searchUnofficialResources('测试')`
  3. 验证返回结果
- **预期结果**：
  - 返回结果数组，长度 > 0
  - 每个结果包含 `source_type: 'unofficial'`
  - episodes 字段包含 m3u8 格式的 URL

**用例 1.2.2：正确提取 m3u8 链接**

- **描述**：验证从 `vod_play_url` 正确提取 m3u8 链接
- **测试步骤**：
  1. Mock 包含 m3u8 链接的响应
  2. 调用搜索函数
  3. 验证 episodes 数组
- **预期结果**：所有 episodes 都是 m3u8 格式

**用例 1.2.3：去重 m3u8 链接**

- **描述**：验证重复的 m3u8 链接被去重
- **测试步骤**：
  1. Mock 包含重复 m3u8 链接的响应
  2. 调用搜索函数
  3. 验证去重结果
- **预期结果**：episodes 数组中没有重复项

**其他用例**：参考官方资源搜索的测试用例（1.1.2 - 1.1.8），逻辑相同但针对非官方资源。

---

### 1.3 新解析 API 测试 (`/api/parse`)

#### 测试文件：`src/app/api/parse/__tests__/route.test.ts`

**用例 1.3.1：成功解析视频 URL**

- **描述**：验证解析 API 正常工作
- **测试步骤**：
  1. Mock 解析 API 返回成功的响应
  2. 调用 GET `/api/parse?url=<videoUrl>`
  3. 验证响应
- **预期结果**：
  - 返回 200 状态码
  - `success: true`
  - `data.m3u8_url` 不为空

**用例 1.3.2：缺少 URL 参数**

- **描述**：验证缺少 URL 参数时的错误处理
- **测试步骤**：
  1. 调用 GET `/api/parse`（无参数）
- **预期结果**：返回 400 状态码，错误信息为"缺少视频 URL 参数"

**用例 1.3.3：解析失败**

- **描述**：验证解析失败时的错误处理
- **测试步骤**：
  1. Mock 解析 API 返回失败响应
  2. 调用解析 API
- **预期结果**：返回 500 状态码，包含错误信息

**用例 1.3.4：m3u8_url 为空**

- **描述**：验证 m3u8_url 为空时的处理
- **测试步骤**：
  1. Mock 响应中 `m3u8_url` 为 null
  2. 调用解析 API
- **预期结果**：返回 500 状态码，表示解析失败

**用例 1.3.5：API 错误响应**

- **描述**：验证解析 API 返回错误状态码时的处理
- **测试步骤**：
  1. Mock fetch 返回 500 错误
  2. 调用解析 API
- **预期结果**：返回 500 状态码，包含错误信息

**用例 1.3.6：请求超时**

- **描述**：验证请求超时处理
- **测试步骤**：
  1. Mock fetch 超时（5 秒）
  2. 调用解析 API
- **预期结果**：返回 500 状态码，包含超时错误

**用例 1.3.7：网络错误**

- **描述**：验证网络错误处理
- **测试步骤**：
  1. Mock fetch 抛出网络错误
  2. 调用解析 API
- **预期结果**：返回 500 状态码，包含错误信息

**用例 1.3.8：使用环境变量配置**

- **描述**：验证从环境变量读取解析 API URL
- **测试步骤**：
  1. 设置 `NEXT_PUBLIC_PARSE_API_URL`
  2. 调用解析 API
  3. 验证请求 URL
- **预期结果**：使用环境变量中的 URL

**用例 1.3.9：URL 编码**

- **描述**：验证视频 URL 正确编码
- **测试步骤**：
  1. 使用包含特殊字符的 URL
  2. 调用解析 API
  3. 验证请求中的 URL 编码
- **预期结果**：URL 正确编码

---

## 2. 集成测试

### 2.1 搜索流式 API 集成测试

#### 测试文件：`src/app/api/search/stream/__tests__/route.test.ts`

**用例 2.1.1：合并官方和非官方搜索结果**

- **描述**：验证搜索结果正确合并
- **测试步骤**：
  1. Mock 官方和非官方搜索都返回结果
  2. 调用流式搜索 API
  3. 验证响应
- **预期结果**：
  - 返回 SSE 流
  - 包含官方和非官方资源的结果

**用例 2.1.2：官方搜索失败时继续**

- **描述**：验证官方搜索失败不影响非官方搜索
- **测试步骤**：
  1. Mock 官方搜索失败，非官方搜索成功
  2. 调用流式搜索 API
- **预期结果**：返回非官方搜索结果

**用例 2.1.3：非官方搜索失败时继续**

- **描述**：验证非官方搜索失败不影响官方搜索
- **测试步骤**：
  1. Mock 非官方搜索失败，官方搜索成功
  2. 调用流式搜索 API
- **预期结果**：返回官方搜索结果

**用例 2.1.4：结果去重**

- **描述**：验证相同 source_type 和 id 的结果被去重
- **测试步骤**：
  1. Mock 返回重复的结果
  2. 调用流式搜索 API
  3. 验证去重结果
- **预期结果**：重复结果被正确去重

---

## 3. 端到端测试场景

### 3.1 搜索功能端到端测试

**场景 1：搜索视频资源**

- **前置条件**：应用正常运行
- **测试步骤**：
  1. 打开搜索页面
  2. 输入搜索关键词（如"测试"）
  3. 点击搜索按钮
  4. 观察搜索结果
- **预期结果**：
  - 搜索结果中包含官方资源和非官方资源
  - 每个结果正确标识资源类型（source_type）
  - 搜索结果流式返回，实时更新

**场景 2：官方资源搜索接口失败**

- **前置条件**：官方资源搜索接口不可用
- **测试步骤**：
  1. 模拟官方搜索接口失败
  2. 执行搜索
- **预期结果**：
  - 非官方资源搜索结果正常显示
  - 应用不崩溃，用户体验正常

**场景 3：非官方资源搜索接口失败**

- **前置条件**：非官方资源搜索接口不可用
- **测试步骤**：
  1. 模拟非官方搜索接口失败
  2. 执行搜索
- **预期结果**：
  - 官方资源搜索结果正常显示
  - 应用不崩溃，用户体验正常

---

### 3.2 解析功能端到端测试

**场景 1：成功解析官方资源**

- **前置条件**：已搜索到官方资源
- **测试步骤**：
  1. 点击播放官方资源
  2. 观察解析过程
  3. 验证播放
- **预期结果**：
  - 显示"正在解析视频..."提示
  - 解析成功后开始播放
  - 播放地址为 m3u8 格式
  - 不去广告（直接播放）

**场景 2：解析失败**

- **前置条件**：已搜索到官方资源
- **测试步骤**：
  1. 模拟解析 API 返回失败
  2. 点击播放官方资源
- **预期结果**：
  - 显示错误提示："视频解析失败，请稍后重试或切换其他资源"
  - 提供重试按钮
  - 不开始播放

**场景 3：解析超时**

- **前置条件**：已搜索到官方资源
- **测试步骤**：
  1. 模拟解析 API 超时（>5 秒）
  2. 点击播放官方资源
- **预期结果**：
  - 显示超时错误提示
  - 提供重试按钮

---

### 3.3 播放流程端到端测试

**场景 1：播放官方资源**

- **前置条件**：已搜索到官方资源
- **测试步骤**：
  1. 点击播放官方资源
  2. 等待解析完成
  3. 观察播放行为
- **预期结果**：
  - 自动调用新解析 API
  - 解析成功后播放
  - 不去广告（直接播放解析后的 m3u8）

**场景 2：播放非官方资源**

- **前置条件**：已搜索到非官方资源
- **测试步骤**：
  1. 点击播放非官方资源
  2. 观察播放行为
- **预期结果**：
  - 不调用解析 API
  - 直接播放 m3u8 地址
  - 自动应用去广告逻辑

**场景 3：去广告功能验证**

- **前置条件**：已搜索到非官方资源
- **测试步骤**：
  1. 播放非官方资源
  2. 检查播放器行为
- **预期结果**：
  - 非官方资源：自动去广告
  - 官方资源：不去广告

**场景 4：资源类型标识**

- **前置条件**：已搜索到资源
- **测试步骤**：
  1. 查看搜索结果
  2. 检查资源类型标识
- **预期结果**：
  - 每个结果正确标识 `source_type`
  - 前端可以区分官方和非官方资源（如果实现了 UI 标识）

---

## 4. 性能测试

### 4.1 搜索性能测试

**用例 4.1.1：搜索响应时间**

- **描述**：验证搜索响应时间符合要求
- **测试步骤**：
  1. 执行搜索操作
  2. 记录响应时间
- **预期结果**：单次搜索响应时间 ≤ 5 秒

**用例 4.1.2：流式返回首结果时间**

- **描述**：验证流式返回的首结果时间
- **测试步骤**：
  1. 执行搜索操作
  2. 记录首结果返回时间
- **预期结果**：首结果时间 ≤ 2 秒

**用例 4.1.3：并发搜索**

- **描述**：验证支持多个用户同时搜索
- **测试步骤**：
  1. 模拟多个并发搜索请求
  2. 验证所有请求都能正常处理
- **预期结果**：所有请求都能正常返回结果

---

### 4.2 解析性能测试

**用例 4.2.1：解析响应时间**

- **描述**：验证解析 API 响应时间
- **测试步骤**：
  1. 调用解析 API
  2. 记录响应时间
- **预期结果**：解析响应时间 ≤ 3 秒

**用例 4.2.2：并发解析**

- **描述**：验证支持多个用户同时解析
- **测试步骤**：
  1. 模拟多个并发解析请求
  2. 验证所有请求都能正常处理
- **预期结果**：所有请求都能正常返回结果

---

## 5. 边界条件测试

### 5.1 数据格式边界测试

**用例 5.1.1：空搜索结果**

- **描述**：验证空搜索结果的处理
- **测试步骤**：
  1. 搜索不存在的关键词
  2. 验证结果
- **预期结果**：返回空数组，不崩溃

**用例 5.1.2：异常数据格式**

- **描述**：验证 API 返回异常数据格式时的处理
- **测试步骤**：
  1. Mock 返回异常格式的数据
  2. 调用搜索函数
- **预期结果**：返回空数组，不崩溃

**用例 5.1.3：超长搜索关键词**

- **描述**：验证超长搜索关键词的处理
- **测试步骤**：
  1. 使用超长搜索关键词（>1000 字符）
  2. 执行搜索
- **预期结果**：正常处理，不崩溃

---

### 5.2 网络边界测试

**用例 5.2.1：网络中断**

- **描述**：验证网络中断时的处理
- **测试步骤**：
  1. 模拟网络中断
  2. 执行搜索/解析操作
- **预期结果**：显示错误提示，不崩溃

**用例 5.2.2：慢速网络**

- **描述**：验证慢速网络下的表现
- **测试步骤**：
  1. 模拟慢速网络（延迟 >1 秒）
  2. 执行搜索/解析操作
- **预期结果**：在超时时间内正常返回或显示超时错误

---

## 6. 兼容性测试

### 6.1 浏览器兼容性测试

**用例 6.1.1：Chrome 浏览器**

- **描述**：验证 Chrome 浏览器兼容性
- **测试步骤**：在 Chrome 中执行所有功能测试
- **预期结果**：所有功能正常工作

**用例 6.1.2：Firefox 浏览器**

- **描述**：验证 Firefox 浏览器兼容性
- **测试步骤**：在 Firefox 中执行所有功能测试
- **预期结果**：所有功能正常工作

**用例 6.1.3：Safari 浏览器**

- **描述**：验证 Safari 浏览器兼容性
- **测试步骤**：在 Safari 中执行所有功能测试
- **预期结果**：所有功能正常工作

**用例 6.1.4：Edge 浏览器**

- **描述**：验证 Edge 浏览器兼容性
- **测试步骤**：在 Edge 中执行所有功能测试
- **预期结果**：所有功能正常工作

---

## 7. 测试执行计划

### 7.1 测试优先级

- **P0（必须测试）**：

  - 搜索功能基本流程
  - 解析功能基本流程
  - 播放流程基本功能
  - 错误处理

- **P1（重要测试）**：

  - 性能测试
  - 边界条件测试
  - 兼容性测试

- **P2（可选测试）**：
  - 压力测试
  - 长时间运行测试

### 7.2 测试环境

- **开发环境**：本地开发服务器
- **测试环境**：独立的测试服务器
- **生产环境**：生产环境（仅做冒烟测试）

### 7.3 测试工具

- **单元测试**：Jest
- **集成测试**：Jest + Supertest（如需要）
- **端到端测试**：手动测试 + 浏览器自动化工具（如需要）

---

## 8. 测试报告模板

### 8.1 测试执行记录

| 用例 ID | 用例名称           | 优先级 | 执行结果 | 备注 |
| ------- | ------------------ | ------ | -------- | ---- |
| 1.1.1   | 成功搜索官方资源   | P0     | ✅ 通过  | -    |
| 1.1.2   | 使用自定义 baseUrl | P1     | ✅ 通过  | -    |
| ...     | ...                | ...    | ...      | ...  |

### 8.2 缺陷记录

| 缺陷 ID | 严重程度 | 描述                    | 状态   | 修复版本 |
| ------- | -------- | ----------------------- | ------ | -------- |
| BUG-001 | 高       | 解析 API 超时处理不正确 | 已修复 | v1.0.1   |
| ...     | ...      | ...                     | ...    | ...      |

---

## 9. 验收标准

### 9.1 功能验收

- [ ] 官方资源搜索接口正常工作
- [ ] 非官方资源搜索接口正常工作
- [ ] 搜索结果正确合并并展示
- [ ] 新解析 API 正常工作
- [ ] 官方资源播放时正确使用新解析 API
- [ ] 非官方资源播放流程保持不变
- [ ] 错误处理和提示正常工作

### 9.2 性能验收

- [ ] 搜索响应时间 ≤ 5 秒
- [ ] 解析响应时间 ≤ 3 秒
- [ ] 支持并发搜索和解析

### 9.3 兼容性验收

- [ ] 环境变量配置正确
- [ ] 浏览器兼容性正常
- [ ] 搜索结果数据结构兼容（新增 source_type 字段）
- [ ] 播放流程正确识别资源类型

---

**文档版本**：v1.0  
**最后更新**：2025-02-01  
**状态**：待执行
