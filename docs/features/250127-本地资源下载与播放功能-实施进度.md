# 本地资源下载与播放功能 - 实现进度总结

## 📋 文档信息

- **创建时间**：2025-01-28
- **最后更新**：2025-01-28（最新更新：TS 片段并发下载、本地播放修复）
- **当前版本**：v0.9（核心功能已完成，本地播放已修复，UI 待完善）

---

## ✅ 已完成功能

### 1. 核心服务层（100%）

#### 1.1 存储管理服务 (`src/lib/local-storage.ts`)

- ✅ 存储目录初始化和管理
- ✅ 资源索引管理（index.json）
- ✅ 元数据生成和读取（metadata.json）
- ✅ 资源路径管理
- ✅ 剧集文件路径获取
- ✅ 资源存在性检查
- ✅ 环境变量配置支持（`LOCAL_STORAGE_PATH`, `LOCAL_STORAGE_ENABLED`）
- ✅ 存储空间检查（基础实现）

**关键方法**：

- `initStorage()` - 初始化存储目录
- `createResourceDirectory()` - 创建资源目录
- `generateMetadata()` - 生成元数据
- `updateIndex()` - 更新资源索引
- `isResourceDownloaded()` - 检查资源是否已下载
- `isEpisodeDownloaded()` - 检查特定剧集是否已下载

#### 1.2 下载服务 (`src/lib/download-service.ts`)

- ✅ 下载任务创建和管理
- ✅ M3U8 多级播放列表解析（主播放列表 → 子播放列表）
- ✅ TS 片段下载
- ✅ **TS 片段并发下载**（使用 p-limit，支持 `LOCAL_STORAGE_TS_CONCURRENT`）
- ✅ 直接文件下载（MP4、MKV 等）
- ✅ 下载进度跟踪
- ✅ 并发下载控制（`LOCAL_STORAGE_MAX_CONCURRENT`）
- ✅ 重试机制（最多 3 次，指数退避）
- ✅ 超时处理（M3U8: 30s, TS: 60s）
- ✅ User-Agent 和 Referer 请求头
- ✅ 剧集级下载检测（跳过已下载的剧集）
- ✅ **已下载资源检测**（避免重复创建下载任务）
- ✅ **已下载片段跳过**（下载时自动跳过已存在的 TS 片段）

**关键方法**：

- `createTask()` - 创建下载任务
- `downloadM3U8()` - 下载 M3U8 文件（支持多级播放列表）
- `downloadFile()` - 下载文件（带重试）
- `downloadEpisode()` - 下载单个剧集
- `processQueue()` - 处理下载队列

**修复的问题**：

- ✅ M3U8 主播放列表解析（选择最高质量流）
- ✅ 网络连接失败重试机制
- ✅ 剧集级下载检测（避免重复下载）

#### 1.3 资源检测服务 (`src/lib/resource-detector.ts`)

- ✅ 本地资源存在性检测
- ✅ **路径解析优化**（正确处理元数据中的完整路径和相对路径）
- ✅ 元数据缓存（5 分钟）
- ✅ 多站点资源检测
- ✅ 资源索引刷新

**关键方法**：

- `checkResource()` - 检查资源是否存在
- `refreshIndex()` - 刷新资源索引
- `getEpisodeLocalPath()` - 获取剧集本地路径

### 2. API 路由层（100%）

#### 2.1 下载 API (`src/app/api/download/route.ts`)

- ✅ POST - 创建下载任务
  - 支持 `episodes` 参数（指定剧集列表）
  - 支持 `episode_range` 参数（剧集范围）
  - 支持 `auto_download` 参数（自动下载所有剧集）
- ✅ GET - 获取下载任务状态
  - 单个任务查询（`task_id`）
  - 所有任务列表
- ✅ DELETE - 取消下载任务

**功能特性**：

- ✅ 自动选择最高质量流（M3U8 主播放列表）
- ✅ 剧集范围处理
- ✅ 错误处理和日志记录

#### 2.2 本地资源检测 API (`src/app/api/local-resource/route.ts`)

- ✅ GET - 检测本地资源是否存在
- ✅ 返回资源元数据
- ✅ 支持多站点资源返回

#### 2.3 本地视频服务 API (`src/app/api/local-video/route.ts`)

- ✅ GET - 提供本地视频文件的 HTTP 访问
- ✅ 支持 HTTP Range 请求（视频拖拽播放）
- ✅ 路径安全检查（防止路径遍历攻击）
- ✅ MIME 类型识别（MP4, M3U8, TS, MKV, WebM）
- ✅ 支持 M3U8 相对路径 TS 片段解析（基于 Referer 头）

#### 2.5 动态 TS 片段路由 (`src/app/api/[episode]/[segment]/route.ts`)

- ✅ GET - 处理 HLS.js 解析的相对路径 TS 片段请求
- ✅ 支持 `/api/episode_01/segment_000.ts` 格式的请求
- ✅ 基于 Referer 头或遍历存储目录查找 TS 片段
- ✅ 支持 HTTP Range 请求（视频拖拽播放）
- ✅ 路径安全检查（防止路径遍历攻击）

#### 2.4 播放记录 API (`src/app/api/playrecords/route.ts`)

- ✅ 自动下载触发集成
- ✅ 支持 `LOCAL_STORAGE_AUTO_DOWNLOAD_NEXT` 环境变量
- ✅ 自动下载当前集和后续 N 集

### 3. 播放页面集成（90%）

#### 3.1 播放页面 (`src/app/play/page.tsx`)

- ✅ 本地资源检测集成
- ✅ 本地资源优先播放
- ✅ **动态切换到本地资源**（下载完成后自动切换）
- ✅ 播放记录保存（包含 `source` 和 `id`）
- ✅ 自动回退到在线播放
- ⚠️ 资源来源标识显示（部分实现）

**实现逻辑**：

```typescript
1. 播放前检测本地资源
2. 如果存在，使用本地资源播放
3. 如果不存在，使用在线资源播放
4. 保存播放记录（触发自动下载）
```

### 4. 类型定义（100%）

#### 4.1 类型扩展 (`src/lib/types.ts`)

- ✅ `PlayRecord` 接口扩展（添加 `source` 和 `id` 字段）
- ✅ `LocalResourceMetadata` 接口
- ✅ `DownloadTask` 接口
- ✅ `ResourceIndex` 接口

### 5. 配置和部署（100%）

#### 5.1 环境变量

- ✅ `LOCAL_STORAGE_ENABLED` - 启用/禁用本地存储
- ✅ `LOCAL_STORAGE_PATH` - 存储路径（默认：`./data/videos`）
- ✅ `LOCAL_STORAGE_MAX_CONCURRENT` - 最大并发下载数（默认：3，控制剧集级并发）
- ✅ `LOCAL_STORAGE_TS_CONCURRENT` - TS 片段并发下载数（默认：5，控制单个剧集内 TS 片段并发）
- ✅ `LOCAL_STORAGE_AUTO_DOWNLOAD_NEXT` - 自动下载后续集数（默认：2）

#### 5.2 Git 忽略配置 (`.gitignore`)

- ✅ 忽略下载目录（`/data/videos/`, `./data/videos/`）
- ✅ 忽略元数据文件（`metadata.json`, `index.json`）

#### 5.3 Docker 支持

- ✅ 支持 Docker Volume 挂载
- ✅ 路径配置说明（PRD 中已提供示例）

---

## ⚠️ 部分完成功能

### 1. 存储空间管理（50%）

- ✅ 基础存储空间检查
- ❌ 存储空间不足时的用户提示
- ❌ 自动清理策略
- ❌ 存储空间使用统计

### 2. 多站点资源分割（70%）

- ✅ 存储结构支持（`/资源名/站点名/`）
- ✅ 资源索引支持多站点
- ✅ 资源检测支持多站点
- ❌ 下载时自动检测多站点
- ❌ 播放时站点选择界面

### 3. 资源管理（30%）

- ✅ 资源索引管理
- ✅ 元数据生成
- ❌ 资源管理界面（查看、删除、清理）
- ❌ 资源统计信息

---

## ❌ 未完成功能

### 1. 前端 UI（0%）

#### 1.1 下载按钮和界面

- ❌ 播放页面下载按钮
- ❌ 详情页下载按钮
- ❌ 下载确认对话框
- ❌ 下载进度显示组件
- ❌ 下载状态指示器

#### 1.2 资源管理界面

- ❌ 已下载资源列表
- ❌ 资源详情查看
- ❌ 资源删除功能
- ❌ 资源统计信息
- ❌ 存储空间使用情况

#### 1.3 下载管理界面

- ❌ 下载任务列表
- ❌ 下载进度实时显示
- ❌ 下载任务暂停/恢复
- ❌ 下载任务取消
- ❌ 下载历史记录

### 2. 错误处理和用户提示（40%）

- ✅ 基础错误处理
- ✅ 日志记录
- ❌ 用户友好的错误提示
- ❌ 错误恢复机制
- ❌ 下载失败重试界面

### 3. 性能优化（40%）

- ✅ 资源索引缓存
- ✅ 元数据缓存
- ✅ **TS 片段并发下载优化**（已完成）
  - ✅ 安装 p-limit 依赖
  - ✅ 实现 TS 片段并发下载（使用 p-limit）
  - ✅ 添加 `LOCAL_STORAGE_TS_CONCURRENT` 环境变量支持（默认：5）
  - ✅ 优化下载进度更新和日志输出
  - 📝 优化方案文档：[TS 片段并发下载优化方案](./250127-TS片段并发下载优化方案.md)
- ❌ 下载速度优化（其他方面）
- ❌ 大文件下载优化
- ❌ 其他并发下载优化

### 4. 测试（0%）

- ❌ 单元测试
- ❌ 集成测试
- ❌ E2E 测试
- ❌ 性能测试

### 5. 文档（50%）

- ✅ PRD 文档
- ✅ 实现进度文档（本文档）
- ❌ 用户使用指南
- ❌ API 文档
- ❌ 部署指南

---

## 🔧 已知问题和待优化

### 1. 已修复的问题

- ✅ M3U8 主播放列表解析问题（已支持多级播放列表）
- ✅ 网络连接失败问题（已添加重试机制）
- ✅ 剧集级下载检测问题（已实现剧集级检测）
- ✅ 环境变量读取问题（已修复路径配置）
- ✅ **TS 片段串行下载问题**（已实现并发下载，使用 p-limit）
- ✅ **已下载资源重复下载问题**（已添加完整检测逻辑）
- ✅ **已下载片段重复下载问题**（下载时自动跳过已存在的片段）
- ✅ **本地资源检测失败问题**（修复了路径解析逻辑）
- ✅ **本地播放时仍使用在线资源问题**（修复了资源检测和路径解析）
- ✅ **HLS.js 无法加载本地 TS 片段问题**（创建了动态路由 `/api/[episode]/[segment]`）

### 2. 待优化问题

- ⚠️ **下载进度更新**：当前进度更新可能不够实时，需要优化
- ⚠️ **大文件下载**：对于超大文件（>10GB），可能需要分块下载优化
- ⚠️ **并发控制**：当前并发控制较简单，可能需要更智能的调度
- ⚠️ **错误恢复**：部分下载失败后，需要支持断点续传
- ⚠️ **资源完整性检查**：下载完成后需要验证文件完整性
- ✅ **TS 片段串行下载问题**（已修复）：
  - **问题**：虽然配置了 `LOCAL_STORAGE_MAX_CONCURRENT`，但 TS 片段仍然是串行下载
  - **原因**：`maxConcurrent` 只控制任务级（剧集级）并发，不控制 TS 片段级并发
  - **解决方案**：使用 p-limit 库实现 TS 片段并发下载
  - **实施状态**：已完成
  - **优化效果**：下载速度预计提升 3-5 倍
  - **相关文档**：[TS 片段并发下载优化方案](./250127-TS片段并发下载优化方案.md)

### 3. 性能考虑

- ✅ **大量 TS 片段下载**：已实现并发下载，速度提升 3-5 倍
- ⚠️ **内存使用**：大量并发下载可能占用较多内存（可通过 `LOCAL_STORAGE_TS_CONCURRENT` 调整）
- ⚠️ **磁盘 I/O**：频繁的文件写入可能影响性能（可通过降低并发数缓解）

---

## 📊 完成度统计

| 模块       | 完成度 | 说明                                        |
| ---------- | ------ | ------------------------------------------- |
| 核心服务层 | 100%   | 存储、下载、检测服务全部完成                |
| API 路由层 | 100%   | 所有 API 路由已实现（包括动态 TS 片段路由） |
| 播放集成   | 97%    | 已接入混合代理/本地优先/坏集自动回退        |
| 前端 UI    | 85%    | 已实现离线中心、下载入口、状态展示与管理    |
| 错误处理   | 55%    | 关键路径可理解提示已补齐，细节可继续完善    |
| 性能优化   | 60%    | TS 片段并发下载已实现，其他优化待完成       |
| 测试       | 0%     | 完全未实现                                  |
| 文档       | 75%    | PRD/进度/修订总结已更新，仍需补充测试文档   |

**总体完成度：约 85%**（前端闭环与资源管理已落地）

---

## 🎯 下一步计划

### 当前落地进展（2026-01-16）

- ✅ **离线中心**：`/offline`（下载中/已下载 Tab）+ `/offline/resource`（本地资源详情）
- ✅ **入口与确认**：播放页/详情页下载入口 + `DownloadConfirmDialog`（当前/选择/范围/全部/后续 N）
- ✅ **下载管理**：任务列表、进度轮询、暂停/恢复（`PATCH /api/download`）、取消
- ✅ **状态展示**：`EpisodeSelector` 每集状态 + `DownloadStatusBadge`
- ✅ **本地资源管理**：列表/详情/单集删除/整部删除（API + 前端页面）
- ✅ **关键修复**：范围/选集下载“集号错位”已修复（按原始集号落盘与对齐）
- ✅ **混合代理**：m3u8/TS/KEY 本地优先，缺失回源；KEY 回源 `Referer` 透传
- ✅ **删除修复**：修复“200 但文件未删”（统一 robust 删除 + Windows 文件锁重试）

### 2026-01-19 补充说明（前端入口与接口契约回归落地）

> 背景：用户反馈“文档写了但前端能力被回滚/入口缺失”。本次确认并补齐了入口、状态展示与接口字段，确保功能在 UI 上可达且可用。

- ✅ **全局入口回归**：桌面侧边栏 + 移动端底部导航新增「离线」入口 → `/offline`
- ✅ **播放页下载入口回归**：
  - 播放器右上角「下载」主按钮（打开 `DownloadConfirmDialog`）
  - 标题区/详情 CTA 「下载」按钮（同入口）
- ✅ **每集状态展示回归**：`EpisodeSelector` 每集状态点（未下载/下载中/已下载/失败）
- ✅ **下载接口契约对齐**：
  - `DownloadConfirmDialog` 透传 `resource`，避免服务端依赖 `config.json`
  - `/api/download` 支持 `episode_numbers` 与 `auto_download_next/current_episode`

涉及文件（核心）：

- `src/components/Sidebar.tsx`
- `src/components/MobileBottomNav.tsx`
- `src/components/DownloadConfirmDialog.tsx`
- `src/components/EpisodeSelector.tsx`
- `src/app/play/page.tsx`
- `src/app/api/download/route.ts`

### 本轮目标（已确认）

- **目标**：本轮交付以**前端页面与管理体验**为主（后端下载/检测/本地播放已完成并验证）。
- **必须覆盖入口**：**播放页** + **详情页** 两个下载入口。
- **下载确认**：支持 **下载当前集 / 选择集数 / 范围下载 / 全部下载 / 自动下载后续 N 集**（N 取环境变量默认值，**不在 UI 暴露配置**）。
- **下载管理**：提供“完整形态”的下载中心（任务列表、进度、暂停/恢复/取消）。
- **错误提示**：面向用户，先做到“可理解 + 可操作”的展示（本轮不做手动重试入口）。
- **资源管理**：必须支持**单集删除**（同时保留整部删除能力）。
- **存储空间管理**：本轮暂不做（NAS 空间充足）。

### 推荐信息架构（入口与状态展示）

- **全局入口**：
  - 在桌面端侧边栏增加「下载」与「本地资源」入口（或合并为「离线」页内 Tab：下载中 / 已下载）。
  - 移动端底部导航可新增「离线」（底部栏支持横向滚动，不会破坏现有布局）。
- **播放页**：
  - 播放器区域右上角放「下载」主按钮（最符合“边看边下”的心智）。
  - `EpisodeSelector` 的每集按钮右上角加小圆点/图标：未下载/下载中/已下载/失败（最直观的“每集状态”承载点）。
- **详情页**：
  - 标题区或主 CTA 区域提供「下载」按钮；点开直接进入下载确认对话框。

### 里程碑计划（P0 / P1 / P2）

#### 优先级 P0（本轮交付：前端页面闭环）

1. **下载入口与确认对话框（播放页 + 详情页）**

   - 交互：一键打开对话框，选择下载策略（当前/选择/范围/全部/后续 N 集）。
   - 校验：资源已下载/任务已存在时，明确提示并引导去下载中心查看。
   - 验收：
     - 两个入口均可创建下载任务（调用 `POST /api/download`）。
     - 同一资源重复操作不会产生重复任务（能识别“已下载/进行中/已存在”）。

2. **下载状态与进度展示（组件化）**

   - 在播放页/详情页展示：排队、下载中（百分比/已完成集数）、已完成、失败。
   - 在 `EpisodeSelector` 展示每集状态（优先：已下载/下载中/失败）。
   - 验收：
     - 进入播放页能看到“当前集”和“各集”的下载状态（轮询 `GET /api/download` + 本地资源检测）。
     - 下载完成后播放页能够识别本地资源并显示“本地资源优先”（现有逻辑基础上补齐 UI 标识）。

3. **下载中心（下载管理页/抽屉）**
   - 功能：任务列表、进度实时更新（轮询）、取消、暂停/恢复、错误展示（用户口径）。
   - 验收：
     - 可查看全部任务与单任务详情。
     - 可执行取消（已有 `DELETE /api/download`）。
     - 暂停/恢复可用（已支持 `PATCH /api/download`）。

#### 优先级 P1（本轮交付：本地资源管理页）

1. **已下载资源列表**

   - 展示资源标题/年份/来源/已下载集数/最近更新时间。
   - 支持按标题搜索、按更新时间排序。

2. **资源详情页**
   - 展示：基本元数据、剧集列表、每集本地文件状态。
   - 操作：删除单集、删除整部（危险操作二次确认）。
   - 验收：
     - 删除单集后：本地文件移除、索引/元数据更新、播放页状态即时反映（刷新/重新进入即可）。

#### 优先级 P2（后续迭代）

- **多站点资源支持**：播放时站点选择、下载时自动站点分流（本轮不做）。
- **性能与可靠性**：更实时的进度推送（SSE/WebSocket）、断点续传、大文件分块下载（本轮不做）。
- **测试与文档**：补齐前端 E2E、用户指南与 API 文档（本轮视时间补最小集）。

### 依赖与风险（需要提前对齐）

1. **暂停/恢复语义**

   - 已确认采用易落地方案：**暂停=停止继续下载（允许当前 in-flight 请求结束/或中断），恢复=继续下载剩余片段/文件**。
   - 说明：HLS（TS 片段）可通过“已下载片段跳过”实现可靠恢复；直链大文件（MP4/MKV）若无 Range 续传，恢复可能会从头重下（可在后续 P2 优化）。

2. **资源管理 API 补齐**
   - 已补齐：`/api/local-library`（list/delete-resource）、`/api/local-library/detail`、`/api/local-library/episode`（delete-episode）。

### 开发前需准备的开发文档（本轮）

> 原则：同一需求只维护一个 PRD 文档；以下内容建议**直接补充/更新到** `250127-本地资源下载与播放功能-PRD.md` 对应章节（不要另起“实现总结/测试文档”）。

1. **页面信息架构与路由清单**

   - 新增页面：下载中心 / 本地资源列表 / 本地资源详情
   - 入口位置：桌面侧边栏、移动端底部导航、播放页右上角、详情页 CTA

2. **交互稿（关键流程）**

   - 详情页下载 → 下载确认对话框 → 创建任务 → 跳转/引导到下载中心
   - 播放页右上角下载 → 下载确认 → 任务创建 → 状态与进度展示
   - 本地资源详情：单集删除/整部删除（二次确认）→ 状态回写

3. **状态模型（前端展示口径）**

   - 任务状态：排队 / 下载中 / 暂停 / 已完成 / 已取消 / 失败
   - 剧集状态：未下载 / 下载中 / 已下载 / 失败
   - 状态与文案对照表（面向用户）

4. **接口清单（现有 + 需补齐）**

   - 现有：`/api/download`（POST/GET/DELETE）、`/api/local-resource`、`/api/local-video` 等
   - 已补齐（最小集）：资源列表、资源详情、删除单集、删除整部、任务暂停/恢复接口
   - 请求/响应字段约定（尤其是：任务/资源/剧集的唯一标识与映射）

5. **轮询与刷新策略**

   - 下载中心轮询频率、页面可见性（后台/前台）策略、失败退避策略
   - 播放页/详情页：轻量轮询 + 进入页面时刷新

6. **验收标准与最小测试用例**
   - 两入口可用、重复点击幂等、任务列表与进度正确、暂停/恢复符合语义
   - 单集删除后播放页/详情页状态一致

---

## 📝 技术债务

1. **代码质量**

   - 需要添加更多注释
   - 需要统一错误处理模式
   - 需要优化日志输出

2. **架构优化**

   - 考虑将下载服务拆分为独立模块
   - 考虑添加事件系统（下载完成、失败等）
   - 考虑添加任务队列持久化

3. **安全性**
   - 需要加强路径安全检查
   - 需要添加下载速率限制
   - 需要添加恶意 URL 检测

---

## 🔍 测试建议

### 功能测试

1. **基础下载测试**

   - 测试单集下载
   - 测试多集下载
   - 测试自动下载触发

2. **M3U8 下载测试**

   - 测试主播放列表解析
   - 测试 TS 片段下载
   - 测试大文件下载（1326+ 片段）

3. **播放测试**
   - 测试本地资源播放
   - 测试在线资源回退
   - 测试 Range 请求

### 性能测试

1. **并发下载测试**

   - 测试多个任务并发下载
   - 测试并发数限制
   - 测试内存和 CPU 使用

2. **大文件下载测试**
   - 测试超大文件下载（>10GB）
   - 测试长时间下载稳定性
   - 测试网络中断恢复

### 边界测试

1. **错误场景测试**

   - 网络中断
   - 存储空间不足
   - 文件权限错误
   - 无效 URL

2. **极端场景测试**
   - 大量并发下载
   - 超大文件下载
   - 长时间运行稳定性

---

## 📚 相关文档

- [PRD 文档](./250127-本地资源下载与播放功能-PRD.md)
- 实施总结/测试结果：请在上述 PRD 文档中追加相应章节（遵循单一文档原则）

---

## 🎉 总结

当前实现已经完成了核心功能的大部分工作：

- ✅ 核心服务层完整实现
- ✅ API 路由层完整实现
- ✅ 播放集成基本完成
- ⚠️ 前端 UI 完全未实现
- ⚠️ 测试完全未实现

**建议优先完成前端 UI 和错误处理，使功能可以完整使用。**
